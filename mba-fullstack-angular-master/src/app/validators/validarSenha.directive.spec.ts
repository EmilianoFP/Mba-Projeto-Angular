import { ValidarSenhaDirective } from './validarSenha.directive';

describe('ValidarSenhaDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarSenhaDirective();
    expect(directive).toBeTruthy();
  });
});
